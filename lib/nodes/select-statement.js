// Generated by CoffeeScript 1.10.0
var SelectStatement, exports, u;

u = require('underscore');

SelectStatement = (function() {
  function SelectStatement(cores) {
    var Nodes;
    this.cores = cores;
    Nodes = require('./nodes');
    if (this.cores == null) {
      this.cores = [new Nodes.SelectCore()];
    }
    this.orders = [];
    this.limit = null;
    this.lock = null;
    this.offset = null;
    this["with"] = null;
  }

  SelectStatement.prototype.initializeCopy = function(other) {
    SelectStatement.__super__.initializeCopy.call(this);
    this.cores = u(this.cores).map(function(x) {
      return u(x).clone();
    });
    return this.orders = u(this.orders).map(function(x) {
      return u(x).clone();
    });
  };

  return SelectStatement;

})();

exports = module.exports = SelectStatement;
